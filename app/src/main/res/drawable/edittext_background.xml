<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el campo está en foco (seleccionado) -->
    <item android:state_focused="true">
        <shape>
            <solid android:color="#2E2E2E"/> <!-- Fondo más claro cuando está en foco -->
            <corners android:radius="8dp" />
            <stroke android:width="2dp" android:color="#5E5E5E" /> <!-- Borde más claro -->
        </shape>
    </item>

    <item>
        <shape>
            <solid android:color="#1E1E1E"/> <!-- Fondo oscuro para el estado normal -->
            <corners android:radius="8dp" />
            <stroke android:width="1dp" android:color="#3E3E3E" /> <!-- Borde gris oscuro -->
        </shape>
    </item>
</selector>
